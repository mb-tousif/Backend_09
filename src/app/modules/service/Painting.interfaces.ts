export type TServiceFilterableOptions = {
  search?: string;
  minPrice?: string | number;
  maxPrice?: string | number;
  name?: string;
  price?: string | number;
  category?: string;
  schedule?: string;
};